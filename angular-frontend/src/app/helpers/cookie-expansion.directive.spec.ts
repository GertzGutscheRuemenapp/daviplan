import { CookieExpansionDirective } from './cookie-expansion.directive';

describe('CookieExpansionDirective', () => {
  it('should create an instance', () => {
    const directive = new CookieExpansionDirective();
    expect(directive).toBeTruthy();
  });
});
